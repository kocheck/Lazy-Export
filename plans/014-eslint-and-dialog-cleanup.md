# 014 — Add ESLint (+ Prettier) and remove the two stray native dialogs

> **Planned at:** `df82c72`, 2026-06-14
> **For the executor:** This is a self-contained handoff plan. You have zero prior context.
> Implement task-by-task, in order. Steps use checkbox (`- [ ]`) syntax — check them as you go.

**Goal:** Give the repo a mechanical convention gate. Add a focused ESLint config (optionally
Prettier) that enforces the conventions already documented in `.cursorrules`, wire a `lint` script +
a CI step, and fix the small set of violations it surfaces — including replacing the two native
`alert`/`confirm` dialogs that slipped past plan 002 with the app's existing inline/toast pattern.

**Why this matters:** `.cursorrules` (lines 29–73, 190–283) documents detailed conventions (no
`any`, explicit return types, no native dialogs after plan 002) but **nothing enforces them** — there
is no `eslint*`/`prettier*` config and no `lint` script, and CI has no lint step. On a repo whose
entire workflow is "separate agents execute handoff plans," an unenforced convention drifts: two
`alert`/`confirm` calls already regressed. A lean ruleset with `no-restricted-globals` makes the
rules real and stops the dialogs from returning.

**Findings addressed:** F2 (no enforced conventions) and F3 (residual native dialogs).

**Effort:** M · **Depends on:** none beyond a green baseline (`npx tsc --noEmit` = 0,
`npx vitest run` green). Best landed **before** plan 016 (docs truth-up), so the "CI runs linters"
line becomes true.

**In scope (only these files may change):**
- `eslint.config.js` (**create** — flat config) and optionally `.prettierrc` / `.prettierignore`
- `package.json` (add `lint` [+ optional `format`] script + the ESLint devDependencies)
- `package-lock.json` (regenerated by the install)
- `.github/workflows/ci.yml` (add a `npm run lint` step)
- `src/ui/components/PresetCreator.tsx` (replace the `alert`)
- `src/ui/components/PresetCreator.test.tsx` (assert inline error instead of `alert`)
- `src/ui/App.tsx` (replace the `confirm`; wrap the `case 'error':` body in a block)
- `src/ui/App.test.tsx` (drive the new delete-confirm affordance)
- Any other file with a trivially auto-fixable lint violation surfaced by the config
- `dist/code.js`, `dist/index.html` (regenerated by `npm run build` — commit them, since `src/**`
  changes)

**Out of scope:** Do **not** introduce broad Prettier reflow churn across the whole tree — scope
formatting so it does **not** rewrite files unrelated to this plan (lint config + targeted fixes
only; add `.prettierignore` or skip `format` if it explodes the diff). Do **not** change
`src/plugin/core.ts` logic. Do **not** silently remove the delete confirmation — replace it with a
real confirm affordance.

---

## Handoff conventions (read first)

- **Base branch:** branch from `production`. Create `advisor/014-eslint-and-dialog-cleanup`. PRs
  target `origin/production`. Do **not** commit to `production`; do **not** push/PR unless told.
- **Commits:** one commit per logical step; sentence-case imperative messages.
- **Drift check first:** run
  `git diff --stat df82c72..HEAD -- src/ui/components/PresetCreator.tsx src/ui/App.tsx package.json .github/workflows/ci.yml`
  and compare the "Current state" excerpts below against the live files. On a mismatch, STOP.
- **Shared STOP conditions:** an excerpt no longer matches HEAD; a gate fails twice after a
  reasonable fix; an out-of-scope file must change; `git status` shows unexpected `dist/` churn
  (anything beyond `dist/code.js` and `dist/index.html`).
- **Index update:** on completion, set this plan's row in `plans/README.md` to `DONE`.
- **Tracked-`dist/` policy:** Tasks 2–3 change `src/**`, so run `npm run build`, commit the
  regenerated `dist/code.js` + `dist/index.html`, and verify a **second** build is a no-op.

---

## Current state (verify before editing)

`src/ui/components/PresetCreator.tsx:53-57` — native `alert`:
```tsx
const handleSave = () => {
  if (!name.trim()) {
    alert('Please enter a preset name');
    return;
  }
```

`src/ui/App.tsx:148-149` — native `confirm`:
```tsx
const deletePreset = (presetId: string) => {
  if (!confirm('Delete this preset?')) return;
```

`src/ui/App.tsx:76-83` — `const error` inside a braceless `case` (`no-case-declarations`):
```tsx
case 'error':
  console.error('❌', msg.message);
  const error = new Error(msg.message);
  if (msg.stack) {
    error.stack = msg.stack;
  }
  setToast({ message: msg.message, type: 'error', error });
  break;
```

**Inline-validation exemplar to copy** for the name field — the `customName` flow in `App.tsx`:
- state + clear-on-edit: `App.tsx:24` (`customNameError`), `:88-94` (validate, set error, early
  return), `:252-255` (clear on change);
- rendered error: `App.tsx:259-267` (`<p className="app__hint app__hint--error" role="alert">`).

`package.json` scripts currently: `dev`, `build`, `build:plugin`, `preview`, `typecheck`, `test`,
`test:ui`, `test:run`, `test:coverage` — **no `lint`**. `.github/workflows/ci.yml` runs checkout →
setup-node → `npm ci` → `typecheck` → `build` → dist-check → `test:run` — **no lint step**.

## Commands you will need

| Purpose   | Command            | Expected on success         |
|-----------|--------------------|-----------------------------|
| Install   | `npm install`      | exit 0, updates lockfile    |
| Lint      | `npm run lint`     | exit 0 after Tasks 2–4      |
| Typecheck | `npx tsc --noEmit` | exit 0                      |
| Tests     | `npx vitest run`   | all green                   |
| Build     | `npm run build`    | writes `dist/`              |

## Tasks

### Task 1: Add ESLint config + scripts (do not fix violations yet)

- [ ] Add devDependencies: `eslint`, `typescript-eslint`, `eslint-plugin-react-hooks` (and
  optionally `eslint-plugin-react`, `eslint-config-prettier` + `prettier`). Use `npm install -D …`.
- [ ] Create `eslint.config.js` (flat config) that:
  - lints `src/**/*.{ts,tsx}`, ignores `dist/`, `node_modules/`, `**/coverage/`, `**/*.css`;
  - extends `typescript-eslint`'s recommended config and `react-hooks` recommended rules;
  - turns ON, at minimum: `no-restricted-globals` (or `no-restricted-syntax`) banning `alert`,
    `confirm`, `prompt`, and `window.open`; `no-case-declarations`; `react-hooks/rules-of-hooks`;
    `react-hooks/exhaustive-deps` (may be `warn`);
  - keep it **lean** — correctness/convention rules, not stylistic noise; relax rules in
    `**/*.test.{ts,tsx}` where defensible (`as` casts, etc.).
- [ ] Add to `package.json` scripts: `"lint": "eslint src"` (+ optional
  `"format": "prettier --write src"`).
- [ ] Run `npm run lint`. **Expected:** it reports exactly the known violations (the `alert`, the
  `confirm`, the `no-case-declarations`, possibly a few `exhaustive-deps` warnings).
  **STOP condition:** if it reports **many** errors (e.g. >40) implying a much larger refactor than
  scoped, STOP and report the counts/categories before mass-fixing.
- [ ] Commit: `"Add ESLint flat config, lint script, and dev dependencies"`

### Task 2: Replace the PresetCreator `alert` with inline validation

**File:** `src/ui/components/PresetCreator.tsx`

- [ ] Add a local `nameError` state and render an inline `<p role="alert">` near the name `Input`,
  mirroring the `customNameError` pattern in `App.tsx:259-267`. Set it (instead of `alert`) in
  `handleSave` when `!name.trim()`, and clear it when the name input changes.
- [ ] `npx tsc --noEmit` → exit 0; `npm run lint` no longer flags this file.
- [ ] Commit: `"Replace PresetCreator alert with inline name validation"`

### Task 3: Replace the App delete `confirm` + fix the case block

**File:** `src/ui/App.tsx`

- [ ] Replace the blocking `confirm('Delete this preset?')` (`:149`) with a **non-blocking**
  confirmation affordance — e.g. a per-card "pending delete" state where the 🗑️ button first arms
  ("Confirm?") and a second click deletes, or a small confirmation `Modal`. Deletion must still
  require an explicit confirm step; do not delete on a single click.
- [ ] Wrap the `case 'error':` body (`:76-83`) in a block `{ … }` to satisfy `no-case-declarations`.
- [ ] `npx tsc --noEmit` → exit 0; `npm run lint` no longer flags these.
- [ ] Commit: `"Replace delete confirm() with inline confirmation and fix case block"`

### Task 4: Clean up any remaining lint errors + add the CI step

- [ ] Auto-fix trivially-fixable remaining errors (`eslint src --fix`); for intentional escapes in
  tests add a targeted `// eslint-disable-next-line <rule> -- <reason>`. `npm run lint` → exit 0.
- [ ] Add a `- run: npm run lint` step to `.github/workflows/ci.yml` (after `typecheck`, before or
  after `build`).
- [ ] Commit: `"Resolve remaining lint errors and add lint step to CI"`

### Task 5: Update tests, rebuild, and update the index

- [ ] Update `src/ui/components/PresetCreator.test.tsx` to assert the inline name error appears (and
  that no `alert` is called). Update `src/ui/App.test.tsx`'s delete flow to drive the new confirm
  affordance instead of stubbing `window.confirm`.
- [ ] `npx vitest run` → all green.
- [ ] `npm run build`; `git status --porcelain dist` shows `dist/code.js` + `dist/index.html`
  modified; a second `npm run build` shows no further change.
- [ ] Commit: `"Update dialog tests and rebuild dist"`
- [ ] Set this plan's row in `plans/README.md` to `DONE`. Commit:
  `"Mark plan 014 done in the plans index"`

## Done criteria

- [ ] `npm run lint` exits 0; CI has a lint step.
- [ ] `grep -rn -e 'alert(' -e 'confirm(' -e 'window.open' src/ui --include='*.tsx' | grep -v '\.test\.'`
  returns nothing.
- [ ] `npx tsc --noEmit` → 0; `npx vitest run` → all green.
- [ ] Deletion still requires an explicit confirmation step (verified by the updated test).
- [ ] `dist/` rebuilt and a second build is a no-op.
- [ ] `plans/README.md` row 014 set to `DONE`.

## STOP conditions

Stop and report if:
- The default ruleset surfaces a large refactor (>40 violations) — report before mass-fixing.
- The delete confirmation cannot be done without a component pattern the repo lacks — propose
  options instead of improvising.
- A "Current state" excerpt no longer matches HEAD (drift), or an out-of-scope file must change.

## Maintenance notes

- Keep the ruleset lean so it stays green for future plan executors; add rules deliberately.
- Reviewer: confirm no behavior change beyond the two dialog UX swaps, and that CI actually **fails**
  if an `alert`/`confirm` is reintroduced.
